{
  "name": "muta",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ kodesage/muta",
    "create-local": "graph create --node http://localhost:8020/ kodesage/muta",
    "remove-local": "graph remove --node http://localhost:8020/ kodesage/muta",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kodesage/muta",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.37.1",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
